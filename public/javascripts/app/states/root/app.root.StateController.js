/**
 * @fileoverview The root view controller. Doesn't do anything at present, we dont even care about injecting $scope
 * since the state is abstract.
 */
goog.provide('app.root.controller');

app.root.controller.StateController = function() {

};